apiVersion: security.istio.io/v1beta1
kind: RequestAuthentication
metadata:
  name: jwt-request-authentication
spec:
  selector:
    matchLabels:
      istio: ingressgateway
  jwtRules:
  - issuer: https://swyftx-local.au.auth0.com/
    outputPayloadToHeader: X-User-Context
    jwksUri: https://swyftx-local.au.auth0.com/.well-known/jwks.json
